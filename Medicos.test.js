import MedicosScreen from "./Medicos";
import { render, fireEvent, waitFor } from '@testing-library/react-native';
import React from 'react';

test('testa se os médicos aparecem na tela', async () => {
  /* const { getByPlaceholderText, getByText, findByText } = render(<MedicosScreen />);
  expect(getByText('Cadastro de Médicos')).toBeTruthy(); */
})